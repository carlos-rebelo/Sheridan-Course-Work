package yourturn5;

public class Start {
    public static void main(String[] args)
    {

        /*  ⁡⁣⁢⁢Part A⁡ :
         * Write a class called Stack that implements the Stackable interface.
         * 
         * Remember that a Stack is a LIFO (Last in, first out structure)
         * 
         * Your output for part A should be:
                White
                Yellow
                Red
                Black
                Blue
         * 
         */

        // Stack stack = new Stack();

        // stack.push(new Car("Blue", "ABC123"));

        // stack.push(new Car("Red", "XYZ321"));

        // stack.push(new Car("Yellow", "AAA111"));

        // stack.push(new Car("White", "ZZZ999"));

        // System.out.println(stack.pop().getColour());

        // System.out.println(stack.pop().getColour());

        // System.out.println(stack.pop().getColour());

        // stack.push(new Car("Black", "LMN001"));

        // System.out.println(stack.pop().getColour());

        // System.out.println(stack.peek().getColour());

        /*  ⁡⁣⁢⁢Part B⁡ :
         * Write a class called GenericStack that works just like Stack but can accept any object instead of just a Car object.  You'll have to use Generics to pull this off! 
         * It seems difficult at first, but it really is quite easy!  Just look at everywhere you used Car in your Stack class, and replace it with a Generic!
         * 
         *If you've completed GenericStack correctly, the following code should work.  Notice that we can push Car objects and we can push Student objects into our GenericStack!
         * 
         */

        // GenericStack<Student> stack2 = new GenericStack<>();

        // stack2.push(new Student("Bart", 123));

        // stack2.push(new Student("Lisa", 134));

        // stack2.push(new Student("Millhouse", 456));

        // System.out.println(stack2.pop().getName());

        // GenericStack<Car> stack3 = new GenericStack<>();

        // stack3.push(new Car("Blue", "ABC123"));

        // stack3.push(new Car("Red", "XYZ321"));

        // stack3.push(new Car("Yellow", "AAA111"));

        // System.out.println(stack3.pop().getColour());

    }
}
