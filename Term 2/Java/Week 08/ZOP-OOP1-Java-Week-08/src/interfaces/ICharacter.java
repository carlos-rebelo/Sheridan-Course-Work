package interfaces;

//ICharacter is an interface.  An interface is like a promise.  If my class implements an interface, they I have made a promise that all of the methods in that interface will be written in my class.

public interface ICharacter {

    void action();

    void speak();
}
