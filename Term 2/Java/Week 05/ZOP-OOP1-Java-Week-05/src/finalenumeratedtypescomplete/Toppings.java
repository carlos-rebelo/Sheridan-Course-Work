package finalenumeratedtypescomplete;

public enum Toppings { //notice that we have replaced class with enum

    /*Hint
    CHEESE(.99), CHICKEN(.99), EXTRA_PINEAPPLE(.01);
    
    private double costOfTopping;
    
    private Toppings(double costOfTopping) {
        this.costOfTopping = costOfTopping;
    }
     */
}
